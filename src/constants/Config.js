import Texts from './Texts';

const bannerConfig = [
  {'label': Texts.title, 'ref': ''},
  {'label': 'Search', 'ref': ''},
  {'label': 'Broadcast', 'ref': ''},
];

const Config = {
  'bannerConfig' : bannerConfig,
};

export default Config;
