import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';

const Login = () => (
  <Card className="container">
    <CardTitle title="Project Clix" subtitle="This is the login page"/>
  </Card>
);

export default Login;
